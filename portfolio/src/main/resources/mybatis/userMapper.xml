<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="io.github.qudgns200.dao.UserDao">
	<select id="selectOneUser" parameterType="String" resultType="io.github.qudgns200.model.User">
		SELECT * FROM USER WHERE EMAIL=#{EMAIL}
	</select>
</mapper>